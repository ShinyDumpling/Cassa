# Screen 选股层第一版方案

## 主题概述

本文件记录 2026-07-09 围绕 Cassa 新版选股层的设计。

用户明确要求新增一个独立选股层：

```text
screen.py
```

新版选股层不沿用旧 `cassa.py screener` 的单体流程，也不围绕旧 screener 的策略注册表继续扩展。它应服务新的数据中心 `data.py`，并允许人或 Agent Skill 用普通 Python 函数灵活组合筛选条件。

## 已确认结论

### 1. 文件命名

选股层入口文件命名为：

```text
screen.py
```

### 2. 一层筛选就是一个函数

选股策略不是一个大函数，也不强制使用 `LAYERS` 注册表。

一层筛选就是一个普通 Python 函数。

标准函数形态：

```python
def filter_xxx(stock_codes):
    """执行某一层筛选。"""
    return remaining_stock_codes
```

入参：

```text
上一层剩下的股票代码列表
```

出参：

```text
这一层筛完后剩下的股票代码列表
```

示例：

```python
stock_codes = get_all_stock_codes()

stock_codes = filter_condition_1(stock_codes)
stock_codes = filter_condition_3(stock_codes)
stock_codes = filter_condition_4(stock_codes)
```

如果要去掉条件 2，就不调用条件 2 对应函数。

如果要新增条件 4，就在本次执行中临时构造 `filter_condition_4` 并调用。

### 3. 策略编排由 Skill 控制

新版选股的核心不是配置系统，而是让 Skill 控制函数调用顺序。

Skill 可以：

1. 选择调用哪些稳定筛选函数。
2. 调整函数调用顺序。
3. 跳过某一层条件。
4. 在本次执行中临时构造一个新的条件函数。
5. 调用 `screen.py` 提供的工具函数打印每层筛选统计。

### 4. 临时条件不写入正式策略 py

用户明确确认：临时新增函数“用完就丢”，不写入正式策略 py，不存在“跑完再删代码”的说法。

因此：

1. 稳定、反复使用、用户确认沉淀的条件，可以进入正式策略脚本。
2. 临时实验条件只服务本次执行，由 Skill 在本次执行过程中临时构造和调用。
3. 临时条件不落入 `strategies/`。
4. 第一版不要围绕“临时写一个 py 再删除”设计流程。

### 5. 股票代码口径

继承数据中心口径：

1. `data.py` 内部使用带后缀通达信代码。
2. `screen.py` 和策略函数也统一使用带后缀股票代码。
3. 业务入口或 Skill 如果接收到纯数字代码，应在业务层补后缀后再进入筛选流程。

示例：

```text
000001.SZ
600519.SH
```

### 6. 初始股票池

选股流程默认从全部 A 股开始。

第一层输入来自：

```python
data.get_stock_list()
```

整理成：

```python
["000001.SZ", "000002.SZ", ...]
```

### 7. 逐层统计

虽然每层函数只返回剩余股票列表，但仍然可以统计每层淘汰数量：

```python
before_count = len(stock_codes)
stock_codes = filter_xxx(stock_codes)
after_count = len(stock_codes)
removed_count = before_count - after_count
```

控制台输出应能看到：

```text
初始股票池：5362
第1层 放量过滤：通过 1200，淘汰 4162
第2层 突破过滤：通过 230，淘汰 970
最终入选：230
```

第一版只要求每层数量统计，不强制记录每只股票的淘汰原因。后续如果需要淘汰原因，再扩展筛选函数返回协议。

## 建议目录结构

第一版建议：

```text
Cassa/
  data.py
  screen.py
  strategies/
    volume_breakout.py
  context/
    2026-07-09-screen.md
```

说明：

1. `screen.py`：选股执行工具与 CLI 入口。
2. `strategies/`：稳定策略脚本目录。
3. 临时条件不放进 `strategies/`。

## screen.py 职责

`screen.py` 不负责具体选股条件本身。

`screen.py` 负责：

1. 从数据中心获取初始全 A 股票池。
2. 提供 `run_filter_layer`，执行单层函数并打印统计。
3. 提供 `run_pipeline`，执行由调用方传入的函数流水线。
4. 提供 CLI 入口，运行稳定策略脚本。
5. 输出最终结果到控制台和 `result/`。

`screen.py` 不负责：

1. 不硬编码放量突破、底背离等具体条件。
2. 不使用 `LAYERS` 注册表。
3. 不从自然语言生成策略。
4. 不把临时条件写入正式策略文件。

## 稳定策略脚本写法

稳定策略脚本是普通 Python 文件。

示例：

```text
strategies/volume_breakout.py
```

里面可以定义多个筛选函数：

```python
def filter_volume_expansion(stock_codes):
    return remaining_stock_codes


def filter_breakout(stock_codes):
    return remaining_stock_codes
```

也可以定义一个 `run()` 函数，手工编排调用顺序：

```python
def run(stock_codes, screen):
    stock_codes = screen.run_filter_layer("放量过滤", filter_volume_expansion, stock_codes)
    stock_codes = screen.run_filter_layer("突破过滤", filter_breakout, stock_codes)
    return stock_codes
```

这里 `screen` 是 `screen.py` 模块本身或一个简单上下文对象，目的是让策略脚本复用 `run_filter_layer` 的统计输出。

## 临时条件的执行方式

当用户通过 Skill 要求临时新增条件时，例如：

```text
在放量突破基础上，临时增加条件：最近 5 日涨幅不能超过 20%。
```

Skill 不应把这个条件写入正式 `strategies/volume_breakout.py`。

正确方式：

1. Skill 读取现有稳定策略函数。
2. Skill 在本次执行上下文中临时构造 `filter_recent_gain_limit(stock_codes)`。
3. Skill 按本次用户要求手工编排：

```python
stock_codes = screen.get_all_stock_codes()
stock_codes = screen.run_filter_layer("放量过滤", filter_volume_expansion, stock_codes)
stock_codes = screen.run_filter_layer("突破过滤", filter_breakout, stock_codes)
stock_codes = screen.run_filter_layer("临时过滤：5日涨幅不超过20%", filter_recent_gain_limit, stock_codes)
```

4. 本次执行结束后，不沉淀该临时函数。

## screen.py 完整代码草案

新增文件：

```text
screen.py
```

完整代码草案如下：

```python
"""
Cassa 选股层入口。

第一版采用函数流水线：一层筛选就是一个函数。
每层函数入参为股票代码列表，出参为剩余股票代码列表。
"""

import argparse
import importlib
import json
from datetime import datetime
from pathlib import Path

import data


PROJECT_ROOT = Path(__file__).resolve().parent
RESULT_DIR = PROJECT_ROOT / "result"


def ensure_result_dir():
    """确保结果目录存在。"""
    RESULT_DIR.mkdir(parents=True, exist_ok=True)


def print_json(value):
    """按 JSON 打印结果。"""
    print(json.dumps(value, ensure_ascii=False, indent=2, default=str))


def get_all_stock_codes():
    """从数据中心获取全部 A 股股票代码。"""
    stock_rows = data.get_stock_list()
    stock_codes = []

    for row in stock_rows:
        if isinstance(row, dict):
            code = row.get("Code", "")
        else:
            code = str(row)

        if code:
            stock_codes.append(code)

    return stock_codes


def normalize_filter_output(layer_name, before_codes, after_codes):
    """校验并规整单层筛选函数返回值。"""
    if after_codes is None:
        raise RuntimeError(f"筛选层 {layer_name} 返回 None，应返回股票代码列表")

    if not isinstance(after_codes, list):
        raise RuntimeError(f"筛选层 {layer_name} 返回值不是 list")

    before_set = set(before_codes)
    normalized_codes = []
    for code in after_codes:
        if code in before_set and code not in normalized_codes:
            normalized_codes.append(code)

    return normalized_codes


def run_filter_layer(layer_name, filter_func, stock_codes, layer_records=None):
    """执行一层筛选，并打印通过 / 淘汰数量。"""
    before_codes = list(stock_codes)
    before_count = len(before_codes)

    after_codes = filter_func(before_codes)
    after_codes = normalize_filter_output(layer_name, before_codes, after_codes)
    after_count = len(after_codes)
    removed_count = before_count - after_count

    print(
        f"[选股] {layer_name}: "
        f"输入 {before_count}，通过 {after_count}，淘汰 {removed_count}"
    )

    if layer_records is not None:
        layer_records.append(
            {
                "layer": layer_name,
                "input_count": before_count,
                "passed_count": after_count,
                "removed_count": removed_count,
            }
        )

    return after_codes


def run_pipeline(stock_codes, filter_steps):
    """按传入顺序执行多层筛选函数。"""
    layer_records = []
    current_codes = list(stock_codes)

    print(f"[选股] 初始股票池：{len(current_codes)}")

    for layer_name, filter_func in filter_steps:
        current_codes = run_filter_layer(
            layer_name=layer_name,
            filter_func=filter_func,
            stock_codes=current_codes,
            layer_records=layer_records,
        )

    print(f"[选股] 最终入选：{len(current_codes)}")
    return current_codes, layer_records


def write_screen_result(strategy_name, selected_codes, layer_records):
    """把选股结果写入 result/。"""
    ensure_result_dir()
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    result_path = RESULT_DIR / f"screen_{strategy_name}_{timestamp}.json"

    result_data = {
        "strategy": strategy_name,
        "created_at": timestamp,
        "selected_count": len(selected_codes),
        "selected_codes": selected_codes,
        "layers": layer_records,
    }

    result_path.write_text(
        json.dumps(result_data, ensure_ascii=False, indent=2, default=str),
        encoding="utf-8",
    )
    print(f"[选股] 结果已写入：{result_path}")
    return result_path


def load_strategy_module(strategy_name):
    """加载 strategies 目录下的稳定策略脚本。"""
    module_name = f"strategies.{strategy_name}"
    return importlib.import_module(module_name)


def run_strategy(strategy_name):
    """运行一个稳定策略脚本。"""
    strategy_module = load_strategy_module(strategy_name)
    if not hasattr(strategy_module, "run"):
        raise RuntimeError(f"策略 {strategy_name} 缺少 run(stock_codes, screen) 函数")

    stock_codes = get_all_stock_codes()
    layer_records = []

    # 给策略脚本一个轻量上下文，避免策略直接 import screen 造成循环引用。
    class ScreenContext:
        def run_filter_layer(self, layer_name, filter_func, codes):
            return run_filter_layer(layer_name, filter_func, codes, layer_records)

    print(f"[选股] 策略：{strategy_name}")
    print(f"[选股] 初始股票池：{len(stock_codes)}")

    selected_codes = strategy_module.run(stock_codes, ScreenContext())

    print(f"[选股] 最终入选：{len(selected_codes)}")
    result_path = write_screen_result(strategy_name, selected_codes, layer_records)
    return {
        "strategy": strategy_name,
        "selected_count": len(selected_codes),
        "selected_codes": selected_codes,
        "layers": layer_records,
        "result_path": str(result_path),
    }


def main():
    """命令行入口。"""
    parser = argparse.ArgumentParser(
        description="Cassa 选股层：按 Python 函数流水线执行逐层筛选。",
    )
    parser.add_argument(
        "--strategy",
        required=True,
        help="策略脚本名称，例如 volume_breakout 对应 strategies/volume_breakout.py",
    )
    parser.add_argument(
        "--json",
        action="store_true",
        help="额外打印完整 JSON 结果",
    )
    args = parser.parse_args()

    data.initialize(Path(__file__))
    result = run_strategy(args.strategy)

    if args.json:
        print_json(result)


if __name__ == "__main__":
    main()
```

## 示例策略脚本代码草案

新增文件：

```text
strategies/example_strategy.py
```

完整代码草案：

```python
"""
示例选股策略。

这个文件只演示一层一个函数，以及 run() 中如何手工编排调用顺序。
真实策略由人或 Skill 根据具体选股逻辑编写。
"""


def filter_first_100(stock_codes):
    """示例条件：只保留前 100 只股票。"""
    return stock_codes[:100]


def filter_even_position(stock_codes):
    """示例条件：只保留偶数位置股票。"""
    return [
        code
        for index, code in enumerate(stock_codes)
        if index % 2 == 0
    ]


def run(stock_codes, screen):
    """执行示例策略。"""
    stock_codes = screen.run_filter_layer("示例：保留前100只", filter_first_100, stock_codes)
    stock_codes = screen.run_filter_layer("示例：保留偶数位置", filter_even_position, stock_codes)
    return stock_codes
```

运行：

```powershell
python screen.py --strategy example_strategy
```

## Skill 草案

以下是用于后续创建 Skill 的完整 `SKILL.md` 草案。

建议 skill 名称：

```text
cassa-screen
```

完整 `SKILL.md`：

```markdown
---
name: cassa-screen
description: 创建、修改和运行 Cassa 选股策略。Use when 用户想写 Python 选股逻辑、增加/删除/调整筛选条件、临时增加一次性筛选条件、运行 screen.py、查看每一层筛选淘汰数量，或需要基于 Cassa data.py 数据中心做选股。
---

# Cassa 选股

使用本 skill 帮用户创建、修改和运行 Cassa 选股逻辑。

## 核心规则

- 使用 `screen.py` 作为选股入口。
- 使用 `data.py` 作为唯一数据来源。
- 一层筛选就是一个 Python 函数。
- 每个筛选函数都必须接收带后缀通达信股票代码列表，并返回筛完后剩余的带后缀股票代码列表。
- 不要要求或引入 `LAYERS` 注册表。
- 用 Python 函数的调用顺序表达策略顺序。
- 尽量使用 `screen.run_filter_layer(layer_name, filter_func, stock_codes)` 执行每层筛选，这样控制台可以打印每层输入数、通过数和淘汰数。
- 选股层内部统一使用带后缀股票代码，例如 `000001.SZ`、`600519.SH`。
- 如果用户提供纯数字代码，应在业务入口层补后缀，再调用 `data.py` 或选股函数。

## 稳定条件与临时条件

稳定条件：

- 稳定、反复使用的条件可以写入 `strategies/` 下的策略文件。
- 函数名要清楚，例如 `filter_volume_expansion`、`filter_breakout_near_high`、`filter_recent_pullback`。
- 每个条件函数只表达一个筛选想法。

临时条件：

- 如果用户要求“本次临时增加某个条件”，不要把它写入正式策略文件。
- 只在本次执行中临时构造和调用这个筛选函数。
- 不要把它描述成“改代码再删除”；它就是一次性执行条件。
- 如果临时条件被验证有价值，且用户明确要求保留，再加入对应策略文件。

## 策略文件模式

稳定策略放在：

```text
strategies/<strategy_name>.py
```

使用这种结构：

```python
def filter_condition_a(stock_codes):
    return remaining_stock_codes


def filter_condition_b(stock_codes):
    return remaining_stock_codes


def run(stock_codes, screen):
    stock_codes = screen.run_filter_layer("条件A", filter_condition_a, stock_codes)
    stock_codes = screen.run_filter_layer("条件B", filter_condition_b, stock_codes)
    return stock_codes
```

如果要删除某个条件，就在 `run()` 中删除或跳过对应函数调用。

如果要调整条件顺序，就调整 `run()` 中函数调用顺序。

## 数据访问

筛选函数中直接使用 `data.py` 提供的数据函数。

常见数据访问方式：

```python
import data

kline_by_code = data.load_daily_kline(stock_codes, count=120)
```

规则：

- 不要在策略文件里直接调用 tqcenter。
- 不要在策略文件里直接读 SQLite。
- 不要创建第二套数据访问路径。
- 过滤大量股票时，优先使用批量数据读取。

## 分层统计

因为每个筛选函数只返回剩余股票代码列表，淘汰数量由 `screen.run_filter_layer` 计算。

除非用户明确要求 debug 细节，否则不要让筛选函数自己打印分层摘要。

预期控制台输出形态：

```text
[选股] 初始股票池：5362
[选股] 放量过滤: 输入 5362，通过 1200，淘汰 4162
[选股] 突破过滤: 输入 1200，通过 230，淘汰 970
[选股] 最终入选：230
```

## 运行稳定策略

运行：

```powershell
python screen.py --strategy volume_breakout
```

如果用户想看完整结构化结果，使用 `--json`：

```powershell
python screen.py --strategy volume_breakout --json
```

## 处理用户请求

当用户要求创建新策略时：

1. 只有在条件不清楚时才追问。
2. 每个条件写成一个函数。
3. 数据只从 `data.py` 获取。
4. 在 `run()` 中手工编排函数调用顺序。
5. 如果用户要求执行，就运行 `python screen.py --strategy <name>`。
6. 汇报每层筛选数量和结果文件路径。

当用户要求删除某个条件时：

1. 找到 `run()` 中对应的函数调用。
2. 删除或跳过该调用。
3. 如果该条件函数是稳定可复用条件，可以保留函数本身。
4. 如果用户要求执行，就运行策略。

当用户要求临时增加条件时：

1. 除非用户明确要求保留，否则不要编辑稳定策略文件。
2. 为本次执行临时构造筛选函数。
3. 用 `screen.run_filter_layer` 执行它。
4. 汇报该条件是临时条件，未保存到正式策略。

## 输出要求

始终保留：

- 初始股票数。
- 每层名称。
- 每层输入数量。
- 每层通过数量。
- 每层淘汰数量。
- 最终入选数量。
- 最终入选股票代码或结果文件路径。

除非用户要求，不要过度解释策略理论。
```

## 后续实施顺序

1. 新增 `screen.py`。
2. 新增 `strategies/` 目录。
3. 新增 `strategies/example_strategy.py`。
4. 用 `python screen.py --strategy example_strategy` 做最小验证。
5. 根据用户确认，再创建真实 `cassa-screen` Skill。
6. 用 Skill 创建第一套真实策略，例如放量突破。

## 暂不做内容

第一版暂不做：

1. 不做 `LAYERS` 注册表。
2. 不做 YAML / JSON 策略配置。
3. 不要求每个筛选函数返回淘汰原因。
4. 不做复杂策略插件系统。
5. 不让临时条件沉淀到正式策略 py。
6. 不把数据读取逻辑写进策略文件之外的第二套数据层。

## 来源会话

来源：2026-07-09 当前会话。
