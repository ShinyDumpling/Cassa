# Cassa 项目任务清单

本文件用于维护当前项目的任务状态，避免想到哪里改到哪里。

## 状态说明

- `todo`: 已确认要做，但还没开始
- `doing`: 正在做
- `blocked`: 暂时被卡住，需要补信息或等决策
- `done`: 已完成
- `dropped`: 已确认暂不做或废弃

## 当前任务

| 任务ID | 状态 | 任务名称 | 目标 | 备注 |
| --- | --- | --- | --- | --- |
| T001 | done | 初始化项目骨架 | 创建 `Cassa` 目录、`cassa.py`、`AGENTS.md`、`README.md`、`context/`、`data/`、`tmp/`、`result/` | 已完成基础目录和规则文件搭建 |
| T002 | done | 建立上下文索引与主题摘要 | 将近期关键会话整理到 `context/`，形成可供新 Agent 阅读的主题材料 | 已生成 `context/README.md` 和 4 份主题摘要 |
| T003 | done | 补充上下文沉淀规则 | 在 `AGENTS.md` 中明确 `context` 的维护方式和索引规则 | 已写入 `AGENTS.md` |
| T004 | done | 补充跨 Agent 一致性规则 | 约束多个 Agent / 会话切换时的代码风格和分析口径一致性 | 已写入 `AGENTS.md` |
| T005 | done | 建立任务管理机制 | 增加 `TASKS.md`，并在 `AGENTS.md` 中约束任务状态维护规则 | 本次完成 |
| T006 | todo | 明确 Cassa 第一版业务范围 | 确认 `cassa.py` 第一版到底保留哪些功能、不保留哪些功能 | 当前虽然已经先接入了 5 个 `tqcenter` 接口，但第一版业务边界仍需单独再收敛确认 |
| T007 | done | 接入通达信 5 个核心接口并统一中文注释规范 | 在 `cassa.py` 中接入 `tqcenter` 的 5 个核心接口，并把代码注释规范明确为中文 | 本次已完成接口接入、最小演示入口、中文注释改写，并在 `AGENTS.md` 中补充中文注释约束 |
| T008 | done | 统一项目内股票代码规范 | 默认在项目内部、打印日志、结果输出中统一使用纯数字股票代码，仅在与通达信接口交互时转换为带市场后缀的代码 | 已实现股票代码标准化、市场后缀推断、纯数字与通达信代码双向转换，并验证 `688318` 与 `688318.SH` 两种输入都能正常运行 |
| T009 | done | 改造命令行为模块化子命令入口 | 支持 `python cassa.py tdx_api <接口名> ...` 的调用方式，并按接口能力区分单股票和多股票参数 | 已完成 `tdx_api` 子命令组；`market_data` 支持多股票代码，其余接口保持单股票代码；已完成真实调用验证 |
| T010 | done | 调整接口输出口径为默认全量返回 | 所有 `tdx_api` 原始接口命令默认返回完整字段，`summary` 在摘要之外也附带完整接口返回 | 已移除默认字段裁剪逻辑；`more_info`、`stock_info`、`market_data` 默认全量返回，`summary` 已验证会附带 5 个接口的完整返回 |
| T011 | done | 收回 `market_data` 的对外暴露 | 不再提供 `tdx_api market_data` 外部命令，也不在 `summary` 的完整接口返回中展示 `market_data` | 已移除 `market_data` 子命令，并从 `summary` 的完整返回块中删除 `market_data`；内部仍可继续复用该接口做摘要计算 |
| T012 | todo | 整理 `summary` 的字段与输出格式 | 重新梳理汇总摘要应该保留哪些字段、按什么顺序输出、哪些内容应收起或展开 | 需要把当前 `summary` 的摘要层和完整返回层重新设计得更清晰、更稳定 |
| T013 | todo | 删除其他通达信接口的对外暴露 | 在确认 `summary` 口径后，进一步评估并移除其余不需要直接暴露给外部使用的 `tdx_api` 子命令 | 当前已删除 `market_data`，后续再决定是否继续收回 `snapshot`、`stock_info`、`more_info`、`relation` |
| T014 | todo | 整理大盘模块第一版边界 | 基于已有上下文，把 `Cassa` 中大盘模块第一版“做什么、不做什么、输出什么”整理清楚，作为后续迁移与重构的任务依据 | 当前已确认：1. 大盘模块属于 `Cassa` 内部模块，不是 `Cassa` 的上游；2. 第一版聚焦市场与板块层面的结构化判断；3. 暂不包含选股；4. 暂不包含盘中盯盘提醒；5. 输出应服务 `Cassa` 内部后续模块消费，而不是只给一段松散描述 |
| T015 | todo | 设计大盘模块内部 JSON 数据结构 | 设计给其他程序消费、debug 打印、后续落库与回测复用的统一 JSON schema | 控制台输出先沿用当前 `Sentinel/market_dashboard.py` 这种分块表格风格；本任务只定义内部结构真源，不直接面向肉眼展示 |
| T016 | todo | 设计大盘模块 Markdown 报告输出 | 设计大盘报告 `.md` 文件的章节结构、字段顺序、展示风格，以及后续接入 LLM 后的承载方式 | 报告文件应独立于控制台输出；控制台先参考 `Sentinel/market_dashboard.py` 的写法，报告层单独沉淀格式 |
| T017 | done | 接入板块列表与板块成分股接口 | 在 `Cassa` 的通达信采集层补齐 `get_sector_list` 和 `get_stock_list_in_sector`，为大盘模块迁移做准备 | 本次已按通达信文档接入两个接口，并补充命令入口与分层注释 |
| T018 | todo | 暂缓接入 `get_stock_list` 与市场广度统计 | 先不接 `get_stock_list`，保留为后续恢复涨跌家数、涨跌停家数、涨跌比等市场广度能力的待办 | 旧版 `market_dashboard.py` 中这部分已注释停用；当前阶段先不接，避免全市场扫描过慢 |
| T019 | doing | 搭建大盘采集主干第一版 | 先完成宽基指数采集、板块列表与板块日 K 批量采集、重点板块成分股采集三块主干，为后续控制台/JSON/md 输出打底 | 当前正在把 `Sentinel/market_dashboard.py` 的核心采集链路迁入 `Cassa`，并补 `market` 正式命令入口；暂不包含市场广度与 LLM 判断 |
| T020 | todo | 确认检查纯数字股票代码映射逻辑 | 系统检查整个项目中“内部纯数字代码 -> 通达信带后缀代码”的映射逻辑是否一致、是否覆盖北交所新旧代码、指数代码与板块代码边界 | 重点核对命令行入口、`market` 主流程、日志输出、内部缓存对象与 TDX 接口边界，避免只修单个接口但全局口径不一致 |
| T021 | todo | 建立板块到方向的映射层 | 把采集到的所有板块进一步映射到更高一层的“方向”，用于控制台展示聚合结果，并为后续 LLM 判断市场主线提供更稳定的结构化输入 | 当前先不做“方向和板块关系类型”的复杂分层，只先做一层方向映射；方向示例包括医疗、科技、金融等 |
| T022 | done | 建立公共 LLM 接入层第一版 | 先按 OpenAI 兼容格式建立可复用的公共 LLM 调用层，统一从环境变量读取配置，供后续多个模块共用 | 已完成公共配置读取、Windows 用户环境变量兼容读取、OpenAI 兼容请求封装，以及 `llm config` / `llm text` 最小命令入口；当前仍暂不兼容 Claude 原生格式 |
| T023 | todo | 规划中间态与结果数据的入库方案 | 系统梳理大盘模块及后续模块产生的中间态数据、结构化结果数据、LLM 输出结果是否需要入库，以及采用什么表结构和保留策略 | 当前先做任务记录，后续结合回测、追踪、报表复盘需求再确定具体数据库和 schema |
| T024 | done | 接入 market 合并版 LLM 判断与调试开关 | 在 `market` 主流程中接入一次性合并的 LLM 风格/方向判断，并支持通过统一参数打印完整 prompt 与原始 LLM 返回 | 已完成单 prompt + 固定 JSON 输出方案、`--debug-llm` 调试开关、`--no-llm` 跳过开关，以及控制台摘要渲染；已验证 `market --no-llm` 能正常运行，但尚未用真实 API key 完整验证模型调用链路 |
| T025 | todo | 研究 B 站引流视频里的选股方法 | 系统收集并拆解 B 站常见引流型炒股视频里的选股话术、筛选条件、判断步骤与隐含假设，整理成可评估的方法清单 | 当前先做研究与归纳，不直接采信；后续再区分哪些适合转成 `Cassa` 的结构化规则、哪些只是流量包装话术 |
| T026 | todo | 重构板块筛选逻辑并补理论基础 | 系统回看当前 `market` 中各类板块筛选策略的定义是否合理，补充更扎实的理论依据、分类框架与筛选原则，再决定是否调整阈值与策略集合 | 当前先记录为待办；现阶段板块筛选逻辑已能辅助观察，但仍偏经验拼接，后续需要先补理论基础，再做更稳的优化 |
| T027 | doing | 选股模块第一版：突破前入场筛选器 | 基于"突破前入场"视频策略，实现底背离+趋势反转+波段位置三步筛选，数据源为 SQLite 日K库，MACD 用通达信公式引擎计算 | 已完成代码编写，待验证运行 |
| T028 | todo | 重设计波段位置中的高位回调判断 | 当前先移除“DIF 近40根高点回落幅度 < 50%”硬过滤，先观察放宽后的人工作业效果，再决定是缩短窗口、改为背离低点后窗口，还是换掉这一判断 | 现阶段先保留 `pullback_ratio` 作为观察字段，不再参与通过/失败判断 |
| T029 | todo | 为 screener 增加历史日期模式 | 让选股逻辑支持按 `as_of_date` 运行，确保某个历史日期的信号只使用该日及之前的数据 | 这是第一阶段历史信号验证的基础；必须避免任何未来数据泄漏 |
| T030 | todo | 实现选股历史区间逐日回放 | 给定 `from/to` 区间，对每个交易日逐日执行一次历史选股，并落下每日信号明细 | 第一阶段先不做交易，只记录当日会选出哪些股票 |
| T031 | todo | 实现 signal_outcomes 涨跌方向统计 | 对每个历史信号记录未来 `1/3/5/10/20` 个交易日的方向结果（`up/down/flat/na`） | 第一阶段明确先不计算收益率百分比，只验证方向胜率 |
| T032 | todo | 输出第一阶段历史信号验证汇总 | 汇总 `total_signal_days`、`total_signals` 以及各窗口的 `valid/up/down/flat/up_rate` | 第一版先验证信号是否更容易上涨，再决定是否进入第二阶段交易回测 |
| T033 | todo | 设计简单交易策略第一版 | 明确选股后的基础交易规则：什么时候买入、什么时候卖出、止盈止损放哪里、单笔最多赚多少、最多亏多少 | 这是第二阶段交易回测的前置设计，先不要和第一阶段历史信号验证混在一起 |
| T034 | todo | 设计选股与大盘/板块联动的两条路径 | 同时梳理两条能力：1. 选股结果如何结合板块分析；2. 从大盘/板块出发，如何在板块内继续选股 | 这项会连接当前 `market` 模块与 `screener` 模块，是后续从“全市场出票”走向“结构化出票”的关键 |
| T035 | done | 建立股票池管理与监控第一版 | 先只做个股层：股票入池、入池理由、后续监控提醒；后面再扩展到板块层 | 已完成第一版：以通达信自定义板块 CASSA 为入口，`python cassa.py stock_pool sync` 同步到本地 SQLite（`data/stock_pool.db`）；TDX 有 DB 无→INSERT，DB 观察 TDX 无→改丢弃；TdxClient 新增 `create_sector` 方法 |
| T036 | done | 个股趋势分析层第一版：纯规则计算 | 对齐 DSA 的 StockTrendAnalyzer，纯 Python 重写均线/乖离率/量能/支撑压力/MACD/RSI/综合评分；MACD 复用通达信公式引擎 | 新增 `TrendAnalysisResult` dataclass（约40字段）+ 11个计算/判断函数 + `analyze_stock_trend` 主入口 |
| T037 | done | 个股报告 CLI 入口与数据采集 | `python cassa.py report --codes` 支持；采集基本信息/当日行情/换手率/基本面/行业概念/资金面 | 新增 `extract_today_quote` / `collect_stock_info` / `format_trend_result` / `run_report`；接口坑点：BlockType/BlockName 首字母大写、换手率直接用 fHSL |
| T038 | todo | 接入 LLM 做个股预期判断 | 在现有纯规则事实基础上，拼 prompt 调 LLM，输出预期方向/成立信号/失效信号/关键价位/风险 | 依赖 T022 公共 LLM 接入层已就绪；设计参考 DSA stock_expectation 的输出结构，合并报告+预期为一步 |
| T039 | todo | 全量接入通达信接口数据 | 把 stock_info / more_info / snapshot 等接口的全量字段都纳入个股报告 | 当前只取了部分字段，很多有价值的数据（如涨停价/跌停价/封单等）未接入 |

## 待整理 / 收件箱

| 编号 | 想法 / 需求 | 说明 |
| --- | --- | --- |
| I001 | 为 `context/` 增加模板文件 | 如果后续 context 文件越来越多，可以补一个统一模板 |
| I002 | 规划 `cassa.py` 主流程结构 | 在正式写业务代码前，先把单脚本内部结构和入口参数定清楚 |
| I003 | 研究全市场成交额历史参照 | 拉取 6 大宽基指数每日总成交额的历史数据，算出当前成交额在历史中的分位（新高/新低/均值），输出一句话结论如"今日成交额处于近 N 日第 X 百分位，市场偏热/偏冷/正常"，为大盘模块提供市场温度判断依据 |

## 已废弃 / 暂不做

| 编号 | 状态 | 内容 | 原因 |
| --- | --- | --- | --- |
| D001 | dropped | 继续在旧的 DSA 改造链路上叠更多个股预期脚本 | 旧项目脚本链过长、中间产物过多、边界混乱，因此决定新开 `Cassa` |
